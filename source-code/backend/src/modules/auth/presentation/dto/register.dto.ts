/**
 * Register DTO — re-exported from the Zod schema for convenience.
 *
 * This file is kept for backward compatibility and to maintain the
 * Presentation Layer DTO structure. The actual type definition and
 * validation logic lives in the corresponding Zod schema.
 *
 * @see registerSchema in ../validators/register.schema.ts
 */
export type { RegisterDto } from "../validators/register.schema";
